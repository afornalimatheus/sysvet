{% extends 'base.html.twig' %}

{% block body %}
    <h1>Novo cliente</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Criar" class="btn btn-success" />
    {{ form_end(form) }}
{% endblock %}
